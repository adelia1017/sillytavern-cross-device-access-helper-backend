# 更新日志

## 0.2.0 - 2026-08-12

- 开放严格收窄的安全应用接口，只接受 `deviceIp` 与固定 `mode`。
- 实现重复键检查、允许字段边界验证、临时文件复验、备份、并发保护和原子替换。
- 实现恢复最近一次助手备份；恢复前自动保存当前配置，不接受用户指定路径。
- 状态接口报告最近备份和“配置已改但尚未重启”的状态。
- 任何写入失败都保留原配置；不执行 Shell、子进程或自动重启。

## 0.1.1 - 2026-08-12

- 修复 Android Node 导入打包入口时 `Dynamic require of "process" is not supported` 的兼容性错误。
- 新增直接运行打包文件并解析 YAML 的回归测试。

## 0.1.0 - 2026-08-12

- 添加只读诊断脚本，检查安装路径、启用状态、入口导入、路由注册和状态读取。
- 删除面向普通用户没有帮助的“测试版/开发版”称呼，直接说明功能边界。

## 0.1.0-dev.1 - 2026-08-12

- 建立独立后端版项目。
- 实现服务器插件缺失/未启用状态提示。
- 实现只读配置、运行时网络状态和访问地址检查。
- 实现严格请求结构的修改预览。
- 写入与恢复接口保持禁用。

## 0.1.0-dev.2 - 2026-08-12

- 统一使用更准确的“后端版”名称。
- 后端未响应界面加入可直接使用的 Android Termux 安装向导和复制按钮。
- 新增独立、手动运行的服务器插件启用脚本；运行时后端仍无权修改 `enableServerPlugins`。
- 移除独立前端入口；用户界面统一由主扩展提供，本仓库仅保留服务器组件。
