{
  "name": "sillytavern-cross-device-access-helper-server",
  "version": "0.1.0-dev.1",
  "description": "Security-focused LAN access configuration assistant for SillyTavern",
  "type": "module",
  "main": "server/index.mjs",
  "scripts": {
    "test": "node --test --test-isolation=none",
    "check": "node --check server/index.mjs && node --check server/config-service.mjs && node --check frontend/index.js && node --check shared/ip-utils.js"
  },
  "dependencies": {
    "yaml": "2.8.3"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "AGPL-3.0-only"
}
