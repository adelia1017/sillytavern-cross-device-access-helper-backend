{
  "name": "sillytavern-cross-device-access-helper-backend",
  "version": "0.2.0",
  "description": "Security-focused LAN access configuration assistant for SillyTavern",
  "type": "module",
  "main": "dist/server-plugin.mjs",
  "scripts": {
    "build": "node scripts/build.mjs",
    "diagnose": "node scripts/diagnose.mjs",
    "test": "node --test --test-isolation=none",
    "check": "node --check server/index.mjs && node --check server/config-service.mjs && node --check scripts/build.mjs && node --check scripts/diagnose.mjs && node --check scripts/enable-server-plugins.mjs && node --check shared/ip-utils.js && node --check dist/server-plugin.mjs"
  },
  "dependencies": {
    "yaml": "2.8.3"
  },
  "devDependencies": {
    "esbuild": "0.25.8"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "AGPL-3.0-only"
}
