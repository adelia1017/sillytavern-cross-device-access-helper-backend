# 测试记录

## 0.1.0-dev.1 自动测试

运行：

```bash
npm install
npm run build
npm run check
npm test
```

覆盖范围：

- RFC1918 私有 IPv4 接受与公网/畸形地址拒绝。
- 请求只允许 `deviceIp`、`mode` 两个字段。
- YAML 重复键与错误字段类型拒绝。
- 非标准 `configPath` 拒绝。
- 已有白名单及 `::1`、`127.0.0.1` 的保留预览。
- `whitelist.txt` 覆盖风险识别。
- 网络接口和实际运行端口的访问地址生成。
- 四个固定路由注册；写入和恢复路由在只读阶段固定返回 501。
- 服务端无子进程、Shell、外部网络模块和敏感数据域引用。
- 手动启用脚本仅支持 Android 与固定路径，拒绝重复键，先备份再原子替换，并保持其他配置语义不变。
- 仓库根目录不存在 `manifest.json`，避免被误装成第二个前端扩展。

Windows 普通权限环境不能创建符号链接时，符号链接拒绝用例会跳过；该用例将在 GitHub Actions 的 Linux 环境继续执行。

## 尚未完成

- Android Termux 真机只读接口测试。
- 写入中途失败、原子替换、备份与恢复测试。
- 二次确认和重放防护测试。
